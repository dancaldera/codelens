{
	"name": "codelens",
	"version": "1.0.0",
	"description": "AI-powered code analysis from screenshots - CodeLens visual code analyzer",
	"main": "dist/src/main.js",
	"scripts": {
		"build": "tsc",
		"start": "npm run build && electron .",
		"typescript-check": "tsc --noEmit",
		"dev": "nodemon --watch src --watch index.html --watch styles --ext ts,html,css --exec \"npm run build && electron .\"",
		"watch": "tsc -w",
		"format": "biome format --write",
		"lint": "biome lint --write",
		"check": "biome check --write",
		"test": "vitest run",
		"test:watch": "vitest",
		"test:coverage": "vitest run --coverage",
		"package": "npm run build && electron-builder build --mac",
		"package-win": "npm run build && electron-builder build --win",
		"package-linux": "npm run build && electron-builder build --linux",
		"package-all": "npm run build && electron-builder build -mwl",
		"prepare": "husky"
	},
	"dependencies": {
		"dotenv": "^17.2.3",
		"openai": "^6.15.0",
		"winston": "^3.19.0"
	},
	"devDependencies": {
		"@biomejs/biome": "2.4.3",
		"@types/glob": "^9.0.0",
		"@types/json-schema": "^7.0.15",
		"@types/node": "^25.0.3",
		"@vitest/coverage-v8": "^4.1.0",
		"electron": "^40.6.0",
		"electron-builder": "^26.0.12",
		"husky": "^9.1.7",
		"lint-staged": "^15.5.0",
		"nodemon": "^3.1.14",
		"typescript": "^5.9.3",
		"vitest": "^4.1.0"
	},
	"lint-staged": {
		"*.ts": "biome check --write"
	},
	"build": {
		"appId": "com.dancaldera.codelens",
		"productName": "CodeLens",
		"files": [
			"dist/**/*",
			"index.html",
			"package.json",
			"styles/**/*"
		],
		"directories": {
			"output": "release/"
		},
		"asar": true,
		"compression": "maximum",
		"mac": {
			"category": "public.app-category.developer-tools",
			"target": [
				"dmg"
			],
			"entitlements": "entitlements.mac.plist",
			"entitlementsInherit": "entitlements.mac.plist",
			"hardenedRuntime": true,
			"gatekeeperAssess": false
		},
		"win": {
			"target": [
				"nsis"
			]
		},
		"linux": {
			"target": [
				"AppImage"
			],
			"category": "Development"
		}
	},
	"author": "Daniel Caldera"
}
