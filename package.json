{
  "name": "electron-overlay-example",
  "version": "1.0.0",
  "description": "Electron overlay window example",
  "main": "dist/src/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron .",
    "start:tools": "DEVTOOLS=true npm run start",
    "typescript-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "electron": "^36.1.0",
    "typescript": "^5.8.3"
  }
}